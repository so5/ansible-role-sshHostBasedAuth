- name: prepend hostbased authentication setting to ssh_config
  blockinfile:
    path: "{{ ssh_hostbased_auth_ssh_config }}"
    insertbefore: BOF
    block: |
      HostBasedAuthentication yes
      EnableSSHKeysign yes
      NoHostAuthenticationForLocalhost yes
